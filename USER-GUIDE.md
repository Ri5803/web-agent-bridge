# 使用指南

## 日常准备

保持安装扩展的浏览器开启，ChatGPT 已登录，扩展已连接本地服务。
在新的 Codex 会话中检查 `web_agent_bridge` 工具是否可用。
这套工具不会把另一个浏览器的登录状态自动转移过来。

## 常用指令

创建并行任务：

> 新建两个网页代理，A 分析方案，B 寻找反例。使用原生中转子代理等待结果并通知主代理。

同会话追问：

> 让 A 在原对话继续分析下面的问题，不要新建对话。

读取结果：

> 列出网页代理和任务状态，并读取已经完成任务的原文结果。

停止与释放名额：

> 停止 B 的当前任务，不自动重发。确认状态后关闭不用的网页代理和原生中转子代理，保留网页历史。

恢复：

> 按保存的代理 ID 恢复原对话，确认状态后再发送新的追问。

默认有 3 个活动网页代理名额。名字方便称呼，但真正用于路由的是
`agentId`；任务由 `jobId` 标识。不要根据名字猜测会话。

## 通知条件

主代理有原生子代理工具时，可把发送和事件等待交给一个只负责传输的
原生中转子代理。中转结束后，宿主的原生完成通知把结果交回主代理。
中转需要额外的子代理名额和模型调用，不是免费的网络回调。

没有原生子代理工具时，仍可直接使用 `web_agent_wait`，但这会阻塞
调用方。不要将其称作向已经结束的主回合主动发送消息。

`notification.configured` 和 `deliveries` 只描述可选 App Server
直连路径，不是原生中转通知的开关。直接 App Server 路径需要用户明确
提供目标线程所属的本地服务器地址与线程 ID；没有默认可用地址。

主回合结束、应用退出、网络断开或机器休眠后的可靠唤醒没有得到保证。

## 故障处理

- 未登录：在安装扩展的浏览器中登录，不要发送密码、验证码或 Cookies。
- 连接失败：检查本地服务、配置的端口、扩展是否启用。
- 令牌错误：使用私有配置中的 `browserToken`，不是 `controlToken`。
- 后台状态不可用：重新加载扩展，关闭旧配置页后重新打开。
- 并发名额不足：关闭不再使用的代理。完成的原生中转也应关闭以释放宿主名额。
- 同一代理正忙：等待当前任务或明确取消，不要并发向同一会话投递。
- 任务超时或断线：先检查原任务是否已提交，再决定是否恢复；不要盲目重发。
- 网页出现限额、验证码或验证提示：由用户处理，不自动绕过。

扩展更新通常需要在浏览器扩展管理页点击重新加载，不必卸载重装。
关闭托管标签页不删除账号中的历史对话。
